# 灵境桌面端 开发文档 v1.0

> **本文档基于实地代码调查撰写**，替代 `~/Desktop/灵境桌面端（LingJing Desktop）PRD v3.0.md` 作为项目权威开发文档。
> **撰写日期**：2026-05-11
> **项目路径**：`/Users/lizhishaoniange/Documents/openclawlingjin/lingjing-desktop/`
> **配套执行计划**：`./PLAN.md`

---

## 0. 文档定位与适用范围

### 0.1 为什么要重写这份文档？

桌面上那份 PRD v3.0 是 AI 基于 Cherry Studio 开源项目凭空生成的，对**本项目的实际代码结构不了解**，存在以下事实性偏差，已在调查中确认：

| PRD v3.0 假设 | 实际情况 |
|---------------|----------|
| `window.lingjing.openclaw.start()` 等嵌套 IPC | 真实 preload 是扁平 API，无 `openclaw.*` 嵌套，无 `on/off` 事件 |
| 事件订阅走 `window.lingjing.on()` | 事件订阅走 Express `/api/events` SSE |
| 大约 7 个 Pinia store | 实际 27 个 store |
| 大约 30 个 Vue 组件 | 实际 25 components + 58 views = 83 SFC |
| `@/` 别名需要新加 | 已存在 |
| mount 节点是 `#root` | 实际是 `#app` |
| 任务模板参考 v2.0 PRD 第 4.5 节 | v2.0 PRD 不在项目内，需重新设计 |

本文档采纳 PRD v3.0 的**产品愿景**和**技术栈选型**，但**所有技术细节以本文档为准**。

### 0.2 适用读者

- 后续接手本项目的开发者（包括 Claude Code、Codex 等 AI 协作者）
- 重构期间需要快速理解项目状态的任何成员

### 0.3 与 PLAN.md 的关系

- **本文档（开发文档.md）**：产品愿景、当前状态、目标架构、技术决策记录
- **PLAN.md**：分阶段实施计划、交付物、验收标准、回滚策略
- 两者配套：本文档回答"是什么、为什么"，PLAN 回答"怎么做、什么时候做"

---

## 1. 产品定位

### 1.1 一句话定义

灵境不是聊天工具，是"AI 帮你操作电脑"的**行动执行平台**。

### 1.2 与 Cherry Studio 的差异

| Cherry Studio 做的（不是重点） | 灵境做的（差异化） |
|---|---|
| 多模型聊天 + 并排对比 | **电脑操作执行**：用户说目标，Agent 自动操作完成 |
| 300+ 预置助手 | **灵境 API 统一计费**：用户零配置 |
| 知识库 RAG | **Agent 市场 + 作者分成**（v1.1+） |
| MCP 工具生态 | 垂直行业 Agent 解决方案 |

### 1.3 产品三层能力

```
第三层（商业化，v1.1+）：Agent 市场 + 灵境 API 计费
第二层（核心差异，v1.0）：电脑操作执行 — Agent 自动完成任务
第一层（基础能力，v1.0）：LLM 对话 + OpenClaw 集成
```

### 1.4 v1.0 范围

**包含**：对话界面、任务执行系统、灵境账号 + 计费、5 个预置 Agent、Onboarding。

**不包含**：Agent 市场、Hermes UI 迁移、知识库 RAG。
- Hermes 子系统在当前 Vue 实现中已存在，但 v1.0 **不迁移到 React**，旧 Vue 代码冻结在 `src-vue-backup/`，主程序不暴露入口。

---

## 2. 当前项目状态盘点（事实）

### 2.1 目录结构

```
lingjing-desktop/
├── electron/
│   ├── main.js                      # Electron 主进程
│   └── preload.cjs                  # 框架无关，contextBridge 暴露 window.lingjing
├── server/                          # Express 后端，4118 行单文件 + 5 个独立模块
│   ├── index.js                     # 65 个端点、12 个路由组
│   ├── database.js                  # SQLite (better-sqlite3)，含 scenarios/tasks/backup
│   ├── gateway.js                   # OpenClaw WebSocket 客户端 + RPC 转发（423 行）
│   ├── hermes-proxy.js              # Hermes AI 系统代理（1431 行，独立 Router）
│   ├── scenarios-routes.js          # /api/wizard/* CRUD（285 行）
│   └── rpc-whitelist.js             # OpenClaw RPC 方法白名单（91 行）
├── src/                             # Vue 3 前端，165 个 TS/Vue 文件，~500KB 源码
├── resources/                       # 内嵌运行时（OpenClaw、Hermes 二进制等）
├── data/                            # SQLite 数据库存放目录
├── tests/                           # Playwright 26 个 spec
├── playwright/                      # 测试 fixtures
├── docs/                            # 历史文档
├── public/
├── index.html                       # mount: #app, script: /src/main.ts
├── vite.config.ts                   # 已有 @/ 别名、/api SSE 代理、VITE_APP_VERSION 注入
├── tsconfig.{app,node}.json         # tsconfig.app extends @vue/tsconfig
├── playwright.config.ts             # baseURL: http://localhost:3001, testDir: ./tests
└── package.json                     # Vue 3.5 + Naive UI + Pinia 全家桶
```

### 2.2 后端真实状态：`server/index.js` 65 个端点 / 12 个路由组

| 路由前缀 | 行范围 | 功能 |
|---|---|---|
| `/api/auth/*` | 312-351 | login/logout/config/check |
| `/api/config` | 382-448 | 读写 .env |
| `/api/health` | 450-456 | 健康检查 |
| `/api/npm/*` | 458-520 | npm 版本/更新 |
| `/api/system/metrics` | 583-660 | CPU/内存/磁盘/网络 |
| `/api/agents/workspace` | 720-744 | Agent 工作目录（v1 会扩展） |
| `/api/files/*` | 754-1175 | list/get/set/mkdir/delete/rename/upload（含 multer） |
| `/api/status` | 1177-1187 | Gateway 状态 |
| `/api/rpc` | 1189-1223 | OpenClaw RPC 白名单转发 |
| `/api/events` | 1225-1253 | SSE 事件广播 |
| `/api/terminal/*` | 1255-1408 | stream/input/resize/destroy/heartbeat（PTY） |
| `/api/hermes-cli/*` | 1534-1827 | Hermes CLI 会话管理 |
| `/api/desktop/*` | 2040-2480 | 桌面显示/输入流（VNC 类） |
| `/api/wizard/*` | 2548-2851 | scenarios/tasks CRUD |
| `/api/media` | 2850+ | 媒体文件访问 |
| `/api/backup/*` | 3636-3900+ | 备份创建/恢复/上传 |

**关键观察**：当前**没有 `/api/lingjing/*` 路由组**。v1 要求的"灵境 API + 账号计费"需要从零新增。

### 2.3 前端真实状态：165 文件 / 27 store / 83 SFC / 26KB+ 的 api 层

**stores/**（27 个 Pinia store，远超 PRD 估的 7 个）：

| 类别 | store |
|---|---|
| 连接 | websocket（gateway 状态/RPC 实例/订阅） |
| 对话 | **chat（37KB，多 agent 状态机）**、session（会话列表/usage） |
| 认证 | auth（灵境用户/token） |
| 监控 | monitor（AgentEvent 队列，max 1000） |
| 主题/语言 | theme、locale |
| 终端/通道 | terminal、channel |
| 业务 | agent、skill、memory、config |
| Hermes 子系统 | stores/hermes/*（12 个 store） |

**composables/**（7 个）：
- `useEventStream.ts` — SSE 事件流，filterByEventType / cleanup
- `useEdgeTTS.ts` — Web Speech API 文本朗读
- `useTheme.ts` — Naive UI darkTheme 切换
- `useTTSSettings.ts` — TTS 配置 localStorage 持久化
- `useConfigEditor.ts`、`useResizable.ts`、`useLingjingTheme.ts` — UI 逻辑

**api/**（26 个文件，~26KB）：
- `rpc-client.ts`（**104KB，核心**）— 20+ RPC 方法封装
- `websocket.ts` — OpenClawWebSocket 类 + EventSource 长连
- `http-client.ts` — Axios 基类 + SSE 自动重连（3s 间隔，最多 20 次）
- `types/{rpc,session,channel,terminal,...}.ts` — TypeScript 类型定义
- `lingjing/{client,auth,chat,tokens,billing,sessions,scenarios}.ts` — 灵境云端 API
- `hermes/*.ts`（4 个）— Hermes 系统配置 schema 和类型

**views/**（58 个 Vue SFC）：Dashboard、Login、Settings、Chat、Monitor、Terminal、Office、Hermes 等。

**components/**（25 个 Vue SFC）：office/、common/、hermes/、layout/。

**i18n/**：zh-CN.ts 121KB + en-US.ts 121KB 翻译字典。

### 2.4 Electron preload 真实 API（**v1 React 端必须按此封装**）

`electron/preload.cjs` 通过 `contextBridge.exposeInMainWorld('lingjing', ...)` 暴露的**真实接口**：

```ts
window.lingjing = {
  // 属性
  platform: string,                              // process.platform
  version: string,                               // Electron 版本

  // OS 安全存储（钥匙串）
  safeStorage: {
    isAvailable(): boolean,
    encrypt(plain: string): string | null,       // base64
    decrypt(b64: string): string | null,
  },

  // 配置 OpenClaw + Hermes
  configureLocalProviders(params: {
    token: string,
    baseUrl?: string,
    modelId?: string,
    providerId?: string,
    compat?: string,
    skipHermes?: boolean,
  }): Promise<{
    openclaw: 'ok' | 'error' | 'skipped',
    hermes: 'ok' | 'error' | 'skipped',
    openclawMessage?: string,
    hermesMessage?: string,
  }>,

  // 自动配置（走 aitoken.homes cookie）
  autoConfigureViaMain(params?: { modelId?: string }): Promise<{
    tokenSource: 'reused' | 'reused-other' | 'created',
    tokenSuffix: string,
    openclaw: 'ok' | 'error' | 'skipped',
    openclawMessage?: string,
    hermes: 'ok' | 'error' | 'skipped',
    hermesMessage?: string,
  }>,

  // Gateway 健康检查与重启
  getGatewayStatus(): Promise<{
    backend: { port: number; alive: boolean },
    openclaw: { port: number; alive: boolean },
    hermes: { port: number; alive: boolean },
  }>,
  restartGateway(which: 'openclaw' | 'hermes'): Promise<{
    status: 'started' | 'already-running' | 'error' | 'skipped' | 'started-but-not-listening',
    port?: number,
    message?: string,
  }>,

  // 外部浏览器
  openExternal(url: string): Promise<{ ok: boolean; message?: string }>,

  // ClawHub 技能市场
  skillsSearch(params?: { query?: string; limit?: number }): Promise<{ ok, results?, message? }>,
  skillsInstall(params: { slug: string; force?: boolean }): Promise<{ ok, stdout?, message? }>,
  skillsInfo(params: { slug: string }): Promise<{ ok, text, code }>,
};
```

**关键事实**：
1. **所有方法都是 invoke/reply Promise**，没有事件订阅 API。
2. **没有 `openExternal` 之外的对话框** — Phase 5（任务执行）需要文件选择对话框时，需要**在 preload 新增** `selectFile` / `selectFolder`，并在 main.js 用 `dialog.showOpenDialog` 实现。
3. 前端"事件订阅"实际是 EventSource 连 Express `/api/events`，不走 IPC。

### 2.5 构建/测试现状

- **Vite**：`@/` 别名已存在；`/api` 代理到 `localhost:3000` 并禁用 SSE 缓冲（**必须保留**）；从 `package.json` 注入 `VITE_APP_VERSION`（**必须保留**）
- **index.html**：mount 节点 `#app`、入口 `/src/main.ts`
- **tsconfig.app.json**：extends `@vue/tsconfig/tsconfig.dom.json`、include 含 `*.vue`（需改）
- **Playwright**：26 个 spec，全部用 CSS class（`.agent-card`、`.chat-bubble`、`.n-modal`）和文本匹配，**无 data-testid** — Naive UI → shadcn 后大量选择器会失效

---

## 3. 目标技术栈

### 3.1 前端栈（重写 src/）

| 角色 | 选型 | 备注 |
|------|------|------|
| 框架 | React 18 + TypeScript | |
| 路由 | react-router-dom 6 | |
| 状态管理 | zustand | 比 Pinia 更轻量，27 个 store 按需逐个移植 |
| UI 组件库 | shadcn/ui（基于 Radix UI）| 自带 dark mode、可定制性强 |
| CSS | Tailwind CSS 3 | |
| 构建 | Vite 7.3（不变） | 仅换 plugin |
| Markdown | react-markdown + remark-gfm | |
| 代码高亮 | react-syntax-highlighter | 同步加载，比 shiki 简单 |
| 数学公式 | KaTeX（保留） | |
| PDF | pdfjs-dist（保留，自己写 viewer） | |
| 终端 | @xterm/xterm + addon-fit（保留） | |
| 图标 | lucide-react | |
| HTTP | fetch + axios（保留 axios 用于 SSE 重连） | |
| 表单 | react-hook-form + zod | |
| Toast | sonner | |
| i18n | react-i18next + i18next | 复用现有翻译字典 |
| 动效 | framer-motion | |

**保留的依赖**（继续使用）：`@xterm/*`、`axios`、`better-sqlite3`、`express`、`highlight.js`、`katex`、`markdown-it`（后端用）、`pdfjs-dist`、`ssh2`、`ws`、`node-pty`、`adm-zip`、`archiver`、`tar`、`unzipper`、`multer`、`edge-tts-client`、`@noble/ed25519`、`check-disk-space`、`js-yaml`、`uuid`

**移除的依赖**：`vue`、`vue-router`、`pinia`、`naive-ui`、`@vueuse/core`、`vue-i18n`、`@vitejs/plugin-vue`、`@vue/tsconfig`、`vue-tsc`、`@vue/test-utils`、`@fortawesome/vue-fontawesome`、`@vicons/ionicons5`、`vue-pdf-embed`

### 3.2 后端栈（保持，但重组）

- Express 5、better-sqlite3、ws、node-pty、multer — 全部保留
- `server/index.js` 4118 行拆分为 12 个 `routes/*.js` + 5 个 `lib/*.js`（详见 PLAN.md Phase 1）
- 新增 `server/routes/lingjing-api.js`（v1 必需）

### 3.3 Electron 栈

- 保持 Electron 36.9.5
- `electron/main.js` 改一行加载路径（已经是 dist/index.html，无需改）
- `electron/preload.cjs` **大部分不变**，仅 Phase 5 新增 `selectFile` / `selectFolder` 桥接

---

## 4. 目标前端目录结构

```
src/
├── main.tsx                         # React 入口（挂到 #app）
├── App.tsx                          # 根组件
├── index.css                        # @tailwind 指令
│
├── pages/
│   ├── chat/ChatPage.tsx
│   ├── tasks/{TaskCenter,TaskExecution}.tsx
│   ├── agents/AgentList.tsx
│   ├── account/{AccountPage,UsagePage}.tsx
│   ├── settings/SettingsPage.tsx
│   ├── onboarding/OnboardingPage.tsx
│   └── (terminal/、channels/、cron/ 等工具页占位)
│
├── components/
│   ├── chat/{ChatInput,ChatMessage,ChatHistory,ModelSelector,TypingIndicator}.tsx
│   ├── tasks/{TaskExecutionPanel,TaskStepCard,ApprovalDialog,TaskTemplateCard,FilePickerDialog}.tsx
│   ├── agents/{AgentCard,AgentBadge,AgentSwitcher}.tsx
│   ├── layout/{AppLayout,Sidebar,TitleBar,StatusBar}.tsx
│   ├── common/{GatewaySwitcher,PdfViewer,TerminalPanel}.tsx
│   └── ui/                          # shadcn 组件（CLI 添加）
│
├── hooks/
│   ├── useChat.ts                   # 对话逻辑（OpenClaw RPC + SSE）
│   ├── useTaskExecution.ts          # 任务执行（OpenClaw tools）
│   ├── useAuth.ts                   # 灵境认证 + token 刷新
│   ├── useEventStream.ts            # 从 composables 移植
│   ├── useEdgeTTS.ts                # 从 composables 移植
│   ├── useIPC.ts                    # 包装 services/ipc.ts
│   ├── useGatewayStatus.ts          # 调 ipc.getGatewayStatus
│   └── useTheme.ts                  # shadcn dark mode
│
├── stores/                          # zustand
│   ├── auth-store.ts
│   ├── chat-store.ts                # 从 stores/chat.ts (37KB) 精简移植
│   ├── session-store.ts
│   ├── agent-store.ts
│   ├── task-store.ts
│   ├── settings-store.ts
│   ├── connection-store.ts
│   └── (其余 20 个 Pinia store 按需移植)
│
├── services/
│   ├── ipc.ts                       # 按真实 preload API 封装（扁平）
│   ├── openclaw-rpc.ts              # 从 api/rpc-client.ts (104KB) 移植
│   ├── openclaw-ws.ts               # 从 api/websocket.ts 移植
│   ├── http-client.ts               # 从 api/http-client.ts 移植
│   ├── lingjing-api.ts              # 从 api/lingjing/* 移植 + 补全
│   ├── hermes-api.ts                # 从 api/hermes/* 移植
│   └── agent-templates.ts           # 5 个预置 Agent 元数据
│
├── lib/{utils,constants,format}.ts
├── types/{message,agent,task,api,electron.d}.ts
├── data/task-templates.ts           # 10 个预置任务模板（Phase 5 重新设计）
├── i18n/{index,messages/zh-CN,messages/en-US}.ts
└── router/index.tsx
```

---

## 5. 核心模块设计

### 5.1 IPC 封装（与 PRD 3.4 节不同，按真实 API）

```ts
// src/services/ipc.ts
// 直接代理 window.lingjing 的扁平接口
export const ipc = {
  get platform() { return window.lingjing.platform; },
  get version()  { return window.lingjing.version; },

  configureLocalProviders: (p: ConfigureLocalParams) =>
    window.lingjing.configureLocalProviders(p),
  autoConfigureViaMain:    (p?: { modelId?: string }) =>
    window.lingjing.autoConfigureViaMain(p),

  getGatewayStatus: () => window.lingjing.getGatewayStatus(),
  restartGateway:   (which: 'openclaw' | 'hermes') =>
    window.lingjing.restartGateway(which),

  openExternal: (url: string) => window.lingjing.openExternal(url),

  skillsSearch:  (p?: SkillsSearchParams) => window.lingjing.skillsSearch(p),
  skillsInstall: (p: SkillsInstallParams) => window.lingjing.skillsInstall(p),
  skillsInfo:    (p: { slug: string })    => window.lingjing.skillsInfo(p),

  safeStorage: {
    isAvailable: () => window.lingjing.safeStorage.isAvailable(),
    encrypt: (s: string)    => window.lingjing.safeStorage.encrypt(s),
    decrypt: (b64: string)  => window.lingjing.safeStorage.decrypt(b64),
  },
};
```

**TypeScript 类型声明**（`src/types/electron.d.ts`）见 PLAN.md §0.2，直接复制。

### 5.2 事件订阅（不走 IPC，走 SSE）

```ts
// src/hooks/useEventStream.ts
// 从 src-vue-backup/composables/useEventStream.ts 移植
// 实质：用 EventSource 连接 /api/events
import { useEffect, useRef, useState } from 'react';
import type { AgentEvent } from '@/types/message';

export function useEventStream(eventTypes?: string[]) {
  const [events, setEvents] = useState<AgentEvent[]>([]);
  const [paused, setPaused] = useState(false);
  const sourceRef = useRef<EventSource | null>(null);

  useEffect(() => {
    const es = new EventSource('/api/events');
    sourceRef.current = es;

    es.onmessage = (msg) => {
      if (paused) return;
      const evt = JSON.parse(msg.data) as AgentEvent;
      if (eventTypes && !eventTypes.includes(evt.type)) return;
      setEvents((prev) => [...prev.slice(-999), evt]); // max 1000
    };

    return () => {
      es.close();
      sourceRef.current = null;
    };
  }, [paused, eventTypes?.join(',')]);

  return { events, paused, setPaused, clear: () => setEvents([]) };
}
```

### 5.3 OpenClaw RPC 客户端

`src/services/openclaw-rpc.ts` 从 `src-vue-backup/api/rpc-client.ts`（104KB）移植。RPC 方法签名**完全保留**，仅去掉 Vue 的 `ref/reactive` 包装：

```ts
// 移植前（Vue 风格）
const state = ref<ConnectionState>('disconnected');
const lastError = ref<string | null>(null);

// 移植后（React 风格 — 状态由调用者管理，client 是纯类）
export class OpenClawRPC {
  private state: ConnectionState = 'disconnected';
  private listeners = new Map<string, Set<Handler>>();
  
  async invoke(method: string, params?: unknown): Promise<unknown> { /* ... */ }
  on(event: string, h: Handler): () => void { /* ... */ }
  // 20+ RPC 方法委托给 invoke
  session = {
    list:   () => this.invoke('session.list'),
    create: (p: CreateSessionParams) => this.invoke('session.create', p),
    /* ... */
  };
  // channel.*, config.*, agent.*, skill.* 等
}

export const openClaw = new OpenClawRPC(); // 单例
```

React 端用 `useState` + `useEffect` 订阅 `openClaw.on(...)` 同步 UI。

### 5.4 灵境 API 后端代理（v1 新增）

`server/routes/lingjing-api.js` 接管以下端点（v1 必需）：

```
POST /api/lingjing/auth/login          { phone, code } → { token, refreshToken, user }
POST /api/lingjing/auth/register       { phone, code, ... } → 同上
POST /api/lingjing/auth/refresh        { refreshToken } → { token }
GET  /api/lingjing/auth/profile        → LingjingUser
GET  /api/lingjing/usage               → { messages, tokens, balance }
GET  /api/lingjing/usage/balance       → { balance, currency }
GET  /api/lingjing/models              → [{ id, name, provider, pricing }]
POST /api/lingjing/proxy/:provider/v1/chat/completions  // 转发到云端
```

云端 base URL 走环境变量 `LINGJING_API_BASE`（默认 `https://api.lingjing.com`）。

### 5.5 OpenClaw 任务执行安全模型（Phase 5）

```
┌──────────────────────────────────────────┐
│         OpenClaw Tools Sandbox            │
├──────────────────────────────────────────┤
│ sandboxPaths: 用户授权的目录白名单       │
│ blockedPaths: 系统目录黑名单（/、~/.ssh）│
│ blocklist:    危险命令模式（rm -rf 等）  │
└──────────────────────────────────────────┘
       ↓ 命令出沙箱 / 触发黑名单
┌──────────────────────────────────────────┐
│         ApprovalDialog (React)            │
├──────────────────────────────────────────┤
│ 显示：操作类型 + 具体命令 + 影响说明    │
│ 按钮：允许 / 拒绝 / 查看详情            │
└──────────────────────────────────────────┘
```

配置存放在 `resources/openclaw/tools.config.json`（**Phase 5 需要先确认 OpenClaw Gateway 当前版本支持该机制**）。

### 5.6 5 个预置 Agent

| Agent | 文件 | 定位 |
|---|---|---|
| 文件管家 | `resources/agents/file-butler.md` | 文件整理/查找/重命名 |
| 文档处理专家 | `resources/agents/doc-expert.md` | PDF/Word/Markdown 转换处理 |
| 数据分析助手 | `resources/agents/data-analyst.md` | Excel/CSV 分析、图表生成 |
| 写作助手 | `resources/agents/writer.md` | 文案/邮件/报告写作 |
| 代码助手 | `resources/agents/coder.md` | 代码生成/重构/调试 |

每个 SOUL.md ≥ 500 字，包含角色定义、能力范围、行为规则、安全约束。

### 5.7 路由结构

```ts
// src/router/index.tsx
export const router = createBrowserRouter([
  {
    path: '/',
    element: <AppLayout />,
    children: [
      { index: true, element: <Navigate to="/chat" /> },
      { path: 'chat',         element: <ChatPage /> },
      { path: 'tasks',        element: <TaskCenter /> },
      { path: 'tasks/:id',    element: <TaskExecution /> },
      { path: 'agents',       element: <AgentList /> },
      { path: 'settings',     element: <SettingsPage /> },
      { path: 'account',      element: <AccountPage /> },
      { path: 'account/usage', element: <UsagePage /> },
      // v1.1+：market、skills、channels、cron、terminal
    ],
  },
  { path: '/onboarding', element: <OnboardingPage /> },
]);
```

### 5.8 侧栏导航（v1）

```
核心区
├── 💬 对话           /chat
├── 🚀 任务中心       /tasks
└── 🤖 我的 Agent     /agents

底部
├── 👤 灵境账号       /account
└── ⚙️ 设置          /settings
```

`v1.1+` 再加：`/market`、`/skills`、`/channels`、`/cron`、`/terminal`。

---

## 6. UI/UX 规范

### 6.1 设计语言

- 极简现代，参考 Linear、Vercel Dashboard、Claude.ai
- 配色由 shadcn CSS 变量管理：
  - 浅色：背景 `hsl(0 0% 100%)`，侧栏 `hsl(0 0% 98%)`，主色 `hsl(222 47% 51%)`
  - 深色：背景 `hsl(222 47% 11%)`，侧栏 `hsl(222 47% 8%)`
- 字体：`Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`
- 圆角：shadcn 默认 0.5rem
- 窗口：frameless + 自定义 TitleBar
- 动效：framer-motion，150-200ms

### 6.2 关键 UI 决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 对话气泡背景 | 不用色块区分 | Claude.ai 风格，左对齐 AI / 右对齐用户 |
| Timeline 组件 | 自己写（shadcn 无内置） | `<ol>` + 竖线 + 步骤卡 |
| 代码块复制按钮 | 自己写（shadcn 无内置） | 右上角浮动按钮 + sonner toast |
| 文件选择 | preload 新增 `selectFile`/`selectFolder` | 当前 preload 没有，Phase 5 加 |
| Playwright 选择器 | 新组件强制 `data-testid` | 减少 class 改名导致的脆弱性 |
| 主题切换 | Tailwind `dark:` class，跟随系统 | shadcn 默认机制 |

---

## 7. 后端架构（Phase 1 拆分后）

```
server/
├── index.js                  # < 200 行，仅 app/中间件/router 挂载
├── lib/
│   ├── auth.js               # isAuthEnabled, checkAuth, authMiddleware
│   ├── path-utils.js         # expandHomePath, safePath
│   ├── sse-utils.js          # sseClients Map + sendEvent
│   ├── session-manager.js    # terminal/hermes-cli/desktop 会话清理
│   └── shell-utils.js        # PTY 进程管理
├── routes/
│   ├── auth.js               # /api/auth/*
│   ├── config.js             # /api/config
│   ├── health.js             # /api/health, /api/status
│   ├── system.js             # /api/system/metrics, /api/npm/*
│   ├── files.js              # /api/files/*
│   ├── terminal.js           # /api/terminal/*
│   ├── hermes-cli.js         # /api/hermes-cli/*
│   ├── desktop.js            # /api/desktop/*
│   ├── backup.js             # /api/backup/*
│   ├── wizard.js             # /api/wizard/*
│   ├── agents.js             # /api/agents/* (Phase 7 扩展)
│   ├── rpc.js                # /api/rpc, /api/events
│   └── lingjing-api.js       # /api/lingjing/* (Phase 6 新增)
├── database.js               # 保留 (Phase 7 加 agents/agent_sessions 表)
├── gateway.js                # 保留
├── hermes-proxy.js           # 保留（1431 行，独立 Router）
├── scenarios-routes.js       # 保留
└── rpc-whitelist.js          # 保留
```

---

## 8. 数据模型扩展（Phase 7）

`server/database.js` 新增表：

```sql
CREATE TABLE IF NOT EXISTS agents (
  id           TEXT PRIMARY KEY,
  name         TEXT NOT NULL,
  emoji        TEXT,
  description  TEXT,
  soul_path    TEXT NOT NULL,      -- resources/agents/xxx.md 相对路径
  category     TEXT,                -- 'system' | 'user' | 'market'
  enabled      INTEGER DEFAULT 1,
  usage_count  INTEGER DEFAULT 0,
  created_at   INTEGER NOT NULL,
  updated_at   INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS agent_sessions (
  id           TEXT PRIMARY KEY,
  agent_id     TEXT NOT NULL,
  session_id   TEXT NOT NULL,        -- 关联 sessions/scenarios
  activated_at INTEGER NOT NULL,
  FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_agent_sessions_agent ON agent_sessions(agent_id);
```

种子数据：5 个预置 Agent，category='system'。

---

## 9. 关键技术决策记录

| # | 决策 | 替代方案 | 选定理由 |
|---|---|---|---|
| D1 | mount 节点保持 `#app` | 改为 `#root`（PRD） | 减少 index.html 改动，避免与 Electron loading.html 等冲突 |
| D2 | 状态库选 zustand | Redux Toolkit、Jotai | 27 个 store 移植量大，zustand API 最简单，与 Pinia 心智模型最接近 |
| D3 | Markdown 用 react-syntax-highlighter | shiki | shiki 异步加载更复杂，对话流式渲染对延迟敏感 |
| D4 | i18n 用 react-i18next | i18next 直接调 | 翻译字典已有 121KB，react-i18next 与字典结构兼容 |
| D5 | 备份策略：`git mv src/ src-vue-backup/` 原地保留 | 删除 + git 历史找回 | 迁移期间需要频繁参考旧实现，原地保留更高效 |
| D6 | 测试策略：Phase 4 起强制 data-testid | 保留 class 选择器 | Naive UI class 全失效，重新设计选择器更可持续 |
| D7 | Hermes v1 不迁移 | 完整迁移 | 1431 行 proxy + 12 个 store + 多个 view，工作量不可控；v1 不暴露入口 |
| D8 | 任务模板 Phase 5 重新设计 | 参考 PRD v2.0 | v2.0 PRD 不在项目内，重新设计更可靠 |
| D9 | preload 新增 `selectFile`/`selectFolder` | 用 input[type=file] + Electron 拦截 | 桌面端原生对话框体验更好 |
| D10 | Phase 1 先做后端拆分 | 直接上 React | 后端 4118 行单文件是长期债，与 React 重写不冲突，先拆能让后续 Phase 改 server 路由时更聚焦 |

---

## 10. 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 27 个 store 移植工作量超估 | 高 | Phase 4-6 延期 | **只移植当前 Phase 实际用到的 store**，其余按需移植 |
| Playwright 26 个 spec 全部失效 | 高 | 测试覆盖归零 | Phase 4 起强制 data-testid，老 spec 逐 Phase 迁移；Phase 1-3 验收不依赖老 spec |
| OpenClaw Gateway 不支持 sandboxPaths/blocklist | 中 | Phase 5 阻塞 | **Phase 5 启动前先验证 Gateway 版本能力**，必要时先升级 Gateway |
| preload 缺 dialog API | 中（已知） | Phase 5 需改 preload | 在 PLAN Phase 5 提前安排 preload 改动 |
| 灵境云端 API 还没准备好 | 中 | Phase 6 阻塞 | 后端先做 mock 模式，前端无感切换 |
| chat-store 37KB 复杂状态机移植出错 | 中 | 对话功能不稳 | 不照搬，**重新设计**精简版（只保留必需字段），按测试驱动逐步加 |
| Hermes UI 用户依赖 | 低 | v1 上线后投诉 | v1 不暴露 Hermes 入口；若用户问，告知 v1.1 安排 |
| 中文文件名/路径 macOS 兼容 | 低 | 偶发 bug | 测试时覆盖中文路径 |

---

## 11. 开发阶段总览

详见 `./PLAN.md`。简要：

| Phase | 内容 | 估时 | 关键交付 |
|-------|------|------|---------|
| 1 | 后端拆分 | 3-5 天 | server/index.js < 200 行 |
| 2 | 备份 + React 脚手架 | 3 天 | Electron 加载 React Hello World |
| 3 | 基础设施层 | 5-7 天 | 路由 + IPC + SSE + i18n + 布局骨架 |
| 4 | 对话界面 | 2 周 | 与 OpenClaw 完整对话流 |
| 5 | 任务执行 | 2-3 周 | 执行 10 个预置任务模板 |
| 6 | 灵境 API + Onboarding | 1.5 周 | 注册→登录→自动配置全流程 |
| 7 | 预置 Agent | 1.5 周 | 5 个 Agent 可切换 |
| **v1.0 总计** | | **约 10 周** | |
| 8 | Agent 市场 + 打磨 | 3 周 | v1.1 |
| 9 | Hermes 迁移（可选）| 2-3 周 | v1.2+ 视用户反馈定 |

---

## 12. 许可证合规

- **OpenClaw**：MIT，自由商用，保留版权声明
- **Cherry Studio**：AGPL-3.0 + 商业授权 — **不 fork、不复制其代码，仅参考产品形态**
- **shadcn/ui**：MIT，自由使用
- **Radix UI**：MIT
- **灵境代码**：自主知识产权

---

## 13. 参考

- 原始 PRD（仅作产品愿景参考，技术细节以本文档为准）：`~/Desktop/灵境桌面端（LingJing Desktop）PRD v3.0.md`
- 实施计划：`./PLAN.md`
- 上一代开发文档（Vue 时期）：`../开发文档.md`（项目父目录，仅作历史参考）
- 跨平台运行环境修复计划（v1.1）：`./docs/superpowers/plans/2026-05-24-runtime-cross-platform-fixes.md`
- 升级 manifest 模板：`./docs/update-manifest-example.json`

---

## 14. v1.1.0 升级实录（2026-05 累积）

> **背景**：v1.0 完成后，本节记录从 Phase 17.10 起到 v1.1.0 发布之间的所有新增 / 修复 / 重构。
> **官方仓库**：`https://github.com/xiaomimi123/ai-lingjing-desktop`（v1.1.0 tag 已 push）

### 14.1 三个独立新功能

#### 14.1.1 AI 绘画（侧栏第三个产品）

- **路由**：`/paint`（→ `/paint/text-to-image`、`/paint/history`）
- **数据流**：调灵境云端 `POST /api/lingjing/playground/generate-image`（cookie session_v2 鉴权，与 chat 同源）
- **模型**：复用 `playground/models` 接口，取 `data.image[]` 分组（当前仅 `gpt-image-2`）
- **历史**：zustand persist，环形缓冲 50 条，只存 URL + 元数据（图像 URL 是灵境 CDN 直链，不存 base64 防 localStorage 爆）

**关键文件**：

| 文件 | 责任 |
|------|------|
| `src/types/painting.ts` | `GenerateImageRequest / GeneratedImage / PaintingRecord` 类型 + 尺寸预设 |
| `src/services/lingjing/painting.ts` | `generateImage()` + `listImageModels()` |
| `src/stores/paint-store.ts` | draft 表单 + currentUrls + history（最多 50 条）|
| `src/hooks/usePaintGeneration.ts` | generate / cancel（含 AbortController）|
| `src/pages/paint/PaintPage.tsx` | 三栏布局（历史 + 画板 + 参数）|
| `src/pages/paint/PaintHistoryPage.tsx` | 网格画廊视图 |
| `src/components/paint/{PaintForm,PaintCanvas,PaintHistoryList,SizeSelector}.tsx` | 子组件 |

**已知 schema 模糊**：generate-image 返回体 `{success, data:{images:[{url}]}}` 是基于 OpenAI 风格 + 灵境实测推断的；后端如返回其他形状，`extractImages()` 双向兼容（顶层数组 / `data.data[]` / `data.images[]`）。

#### 14.1.2 升级检查 + R2 发布工作流

**用户侧**（前端）：

- 启动延迟 5s 调一次 `/api/update/check`（`useUpdateCheck`）
- 有新版 → 顶部 `UpdateBanner`（可 24h snooze / 永久忽略该版本）
- 设置 → 关于 → `UpdateCheckCard`：当前版本、最新版本、release notes、"去下载"按钮跳浏览器

**后端**：

- `server/routes/update.js` 拉 `LINGJING_UPDATE_MANIFEST_URL` 上 `latest.json` 比 semver
- 5 分钟缓存（`?force=1` 绕缓存）
- 平台匹配 `darwin-arm64` / `darwin-x64` / `win-x64`
- env：`LINGJING_UPDATE_MANIFEST_URL=https://pub-xxx.r2.dev/latest.json`

**发布脚本** `scripts/release.mjs`：

```bash
npm run release:mac    # 构建 dmg + 上传 R2 + 生成 latest.json + 上传
npm run release:win    # 同上，Win
npm run release:both   # 双平台
npm run release:dry    # dry-run 验证
```

依赖 `wrangler login`。支持 `--skip-build / --notes / --dry-run / --bucket / --public-url`。

**R2 配置**（一次性手工）：

1. Cloudflare dashboard → R2 → 创建 bucket（如 `lingjing-updates`）
2. Settings → Allow Public Access → 拿到 `pub-xxxxxxxx.r2.dev` URL
3. 上传初版 `latest.json`（模板见 `docs/update-manifest-example.json`）
4. `.env` 填 `LINGJING_UPDATE_MANIFEST_URL`

#### 14.1.3 错误报告日志卡

- 自动捕获三路：`window.onerror` / `unhandledrejection` / IPC `runtime-progress` (stage=error/download-failed/install-failed)
- 环形缓冲 50 条，**不持久化**（每次启动清零，避免隐私 + localStorage 占用）
- "复制全部"输出 markdown，含版本 / 平台 / 时间 / 来源分组计数 / 单条 stack+context

**关键文件**：

| 文件 | 责任 |
|------|------|
| `src/stores/error-log-store.ts` | zustand store + `push/clear` |
| `src/hooks/useErrorLogCapture.ts` | App.tsx 启动挂监听 |
| `src/components/settings/ErrorLogCard.tsx` | UI 卡 |

### 14.2 跨平台运行环境一揽子修复（11 task plan）

详细计划：`./docs/superpowers/plans/2026-05-24-runtime-cross-platform-fixes.md`

| ID | 问题 | 修复点 |
|----|------|--------|
| **A1** | bundled Node 装完后 backend 仍在 crash 状态 → 死锁 | `electron/main.js` runtime-ensure-node handler 装完调用 `restartBackendForNode()`：stopBackend → 等 exit → startBackend → pingTcp |
| **A2** | 系统 node v14/v16/v18 让 needsSetup=false 跳过 onboarding，但 openclaw 启动失败 | `runtime-installer.js` 加 `probeNodeVersion()` (3s timeout)；`runtimeStatus` 加 `nodeVersionOk` 字段；`MIN_NODE_MAJOR=22` |
| **A3** | backend crash 后 main.js 不重启 | `on('exit')` 短命退出 < 5s 视为启动失败，退避 2/4/8s × 3，3 次后 emit `lingjing:backend-status restart-exhausted` |
| **B1** | npm install openclaw 几分钟内不可取消 | `ensureBundledNode/OpenClaw` 接 `signal: AbortSignal`；`downloadStream` 关键点 throwIfAborted + listen abort destroy req；npm spawn 后 listen abort 发 SIGTERM；新 IPC `lingjing:runtime-cancel`；UI 加"取消"按钮 |
| **B2** | Welcome 窗口空白等 25s 看像挂了 | 新 `src/pages/welcome/WelcomePage.tsx` 路由 `/welcome`，轮询 /api/health 显示启动进度 |
| **C1+C2** | 侧栏 OpenClaw / Hermes 永远显示，未装时点进去就报错 | 新 `src/stores/runtime-store.ts`（zustand 缓存 status）；Sidebar 接 `isOpenclawAvailable/isHermesAvailable` 派生函数；未装态灰显 + 琥珀点 + 跳 /settings；SecondaryPanel 加 `'未装机'` badge |
| **D1** | SOUL 模板 mac 命令（open / pbcopy），Win 用户 Agent 跑失败 | `electron/platform.js` 加 `platformCommandHints()` 函数版 + `platformCommandHintsSerializable()` IPC 字符串模板版；IPC `lingjing:platform-hints`；`resources/agents/lingjing.md` 补平台备注 |
| **D2** | Hermes Windows 没适配，UI 还显示入口 | `src/lib/platform.ts` 加 `isWindowsPlatform/isMacPlatform`（ipc.platform 优先 + navigator 兜底）；`runtime-store` `isHermesAvailable` Win 上 false；`Sidebar getProducts()` Win 过滤 hermes；router `/hermes/*` 三条规则 Win 重定向到 `/openclaw/chat` |
| **E1** | 硬编码 npmmirror 优先，国外用户慢 5-10 倍 | `runtime-installer.js` 加 `detectFastestMirror(signal)` 并行 HEAD 探测 1.5s 拍板，胜出者作为 sources 首选 |
| **E2** | 公司/学校代理网络下首启完全跑不动 | `downloadStream` 检测 HTTPS_PROXY 打日志（v1.2 接 https-proxy-agent）；`ensureBundledOpenClaw` 的 npm spawn env 透传 `npm_config_proxy/https_proxy` |
| **F1** | backend 起不来主窗空白没提示 | 新 `useBackendHealth` hook 轮询 /api/health（5s + 3s fetch timeout + AbortController + unmount abort），3 次失败认为 down；新 `BackendHealthBanner` 红色顶栏 + "重试"按钮 reload；AppLayout 挂入 |

**Subagent-driven 流程**：每个 Task 由 implementer 子代理实现 → 派 spec reviewer 审 → 派 code quality reviewer 审 → 不通过回送修 → 通过 commit → 下一个 Task。共 19 个 commits 跑完 11 个 task（含 8 个 followup 修 review 提出的问题）。

### 14.3 灵境 logo 重设计

- `public/lingjing.svg`：从纯蓝方块 + 灵字 → 深青 #0E3A5C → 藏紫 #4A2E6C 线性渐变 + 顶左青色光晕 + 4 角微星点 + 灵字白→浅蓝渐变
- `src/components/layout/Sidebar.tsx`：顶部 Link 从内联文本"灵"换成 `<img src="/lingjing.svg">`，跟 OpenClaw/Hermes 视觉一致

### 14.4 生产环境收尾

#### 14.4.1 菜单锁定（隐藏 DevTools + Reload）

`electron/main.js:buildAppMenu` 视图 submenu 把 `reload / forceReload / toggleDevTools` 三项包到 `if (isDev)` 分支：

```js
{
  label: '视图',
  submenu: [
    ...(isDev ? [
      { role: 'reload', label: '刷新' },
      { role: 'forceReload', label: '强制刷新' },
      { role: 'toggleDevTools', label: '开发者工具' },
      { type: 'separator' },
    ] : []),
    { role: 'resetZoom' }, { role: 'zoomIn' }, { role: 'zoomOut' },
    { type: 'separator' },
    { role: 'togglefullscreen' },
  ],
}
```

Electron 内置 role 的快捷键绑定跟菜单项走，菜单移除后 Cmd+Opt+I / Cmd+R 自动失效，**不需要额外 `before-input-event` 拦截**。

#### 14.4.2 HermesSessionPanel button-in-button 修复

`SessionList.tsx:353` 外层 button（整行点击区）内嵌 DropdownMenuTrigger（也是 button），React DOM 警告。改为 `<div role="button" tabIndex={0} onKeyDown={Enter|Space → onClick}>`，保留键盘可访问性。

#### 14.4.3 TSC 严格收尾

`src/services/ipc.ts` browser-mode fallback shape 漏 `message` 字段，导致 5 处 `.message` 访问 TS2339。fix：fallback 加 `message: '浏览器环境不支持'` 让 union shape 跟 `RuntimeStatus.message?: string` 对齐。`npm run build`（`tsc -b && vite build`）现在干净通过。

### 14.5 仓库整理（关键背景）

**事实**（git 实证）：

| repo | URL | 用途 |
|------|-----|------|
| `origin` | `xiaomimi123/OpenClaw-Admin` | 本地工作仓的 origin（v1.0 React 重构的"摇篮"，dev-lingjing 共祖 `d722891`）|
| `upstream` | `itq5/OpenClaw-Admin` | 上游官方（与 origin 同步）|
| `lingjing` | `xiaomimi123/openclaw-Hermes` | 早期备份仓，main 是 Initial commit |
| **`ai-lingjing-desktop`** | **`xiaomimi123/ai-lingjing-desktop`** | **真主线仓**（公开，发了 v1.0.0 + v1.1.0-alpha.1~8）|

本地 `dev-lingjing` 分支在 2026-05-12 时被"清洁打包"推到了 `ai-lingjing-desktop/main`（commit message 一样但 SHA 不同），之后本地继续干活 31 个 commit 没回推。**v1.1.0 发布时**通过 `git worktree` + 31 cherry-pick（零冲突）回填到 `ai-lingjing-desktop`，加 `chore: bump 1.0.0 → 1.1.0` commit，rebase merge 进 main，tag `v1.1.0` 并发布 GitHub Release。

**之后开发约定**：所有改动都直接推 `ai-lingjing-desktop`。本地 `dev-lingjing` 分支作为归档保留。

### 14.6 工程基础设施

#### 14.6.1 git 仓状态机

```
A. 修 bug / 加功能：
   - 直接在 ai-lingjing-desktop/main 上做 feature branch
   - 或在 dev-lingjing 做完 cherry-pick 过去

B. 发版：
   1. bump package.json version
   2. push 到分支 / 建 PR → rebase merge main
   3. git tag -a vX.Y.Z <commit> -m "..."
   4. git push ai-lingjing-desktop vX.Y.Z
   5. gh release create
   6. 跑 npm run release:mac / release:win 上传 dmg/exe 到 R2
   7. 更新 latest.json 版本

C. 用户升级：
   - 启动 5s 后前端检查 /api/update/check → R2 manifest
   - hasUpdate=true → Banner 提示 → 点"去下载"跳浏览器
```

#### 14.6.2 verify 模式

| 检查 | 命令 | 阻塞合并 |
|------|------|----------|
| TSC | `npx tsc -b --noEmit` | ✅ |
| Vite build | `npx vite build` | ✅ |
| Node syntax | `node --check electron/*.js` | ✅ |
| 完整 build | `npm run build` (tsc + vite) | ✅ |
| Playwright E2E | `npm run test:e2e` | ⚠️ 改 Phase 14+ 后跟运行中 electron 强耦合，建议在新 worktree 跑 |

#### 14.6.3 长寿运行时检查

- vite + 后端 + electron 通过 `npm run electron:dev` 一条龙起
- 改 electron/main.js 需要重启 electron（HMR 不覆盖 main 进程）
- 改前端 src/ → vite HMR 自动应用
- 后端 backend (server/index.js) 通过 main.js spawn，改后需要 `pkill -f "server/index.js"` 让 main.js 自动重启（A1 + A3 已在 v1.1.0 实现）

### 14.7 v1.1.0 不在范围（v1.2+）

- Linux 打包
- Apple 公证 + Windows 代码签名（dmg/exe 仍 unsigned）
- 自动更新机制（auto-update 走 electron-updater，v1.1 仅检查 + 跳浏览器下载）
- Hermes Windows 适配
- HTTPS_PROXY 真走代理（v1.1 仅给 npm 透传 + console 提示，下载 Node 仍直连）
- 错误日志上报到云端（v1.1 仅本地复制粘贴）
- 设置页"跳进设置"时滚动锚定到 RuntimeStatusCard（v1.1 仅跳 /settings 根）

---

*文档结束。如发现与代码实情不符，请优先修正本文档。*
